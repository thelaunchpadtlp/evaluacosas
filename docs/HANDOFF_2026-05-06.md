# Handoff — 2026-05-06 (sesión gigante)
**Estado**: producción operativa · todos los flujos críticos verdes · cero deuda urgente

---

## TL;DR for-dummies

Lo que **funciona en vivo** ahora mismo:

| URL | Qué es |
|---|---|
| `evaluacosas.thelaunchpadtlp.education` | Catálogo + landing |
| `evaluacosas.thelaunchpadtlp.education/dashboard/` | Dashboard del docente con OAuth funcionando |
| `evaluacosas.thelaunchpadtlp.education/biologia-10/` | Tarea integral 80 ítems |
| `evaluacosas.thelaunchpadtlp.education/ayuda/` | Centro de ayuda for-dummies |
| `evaluacosas.thelaunchpadtlp.education/sitemap/` | Mapa del sitio auto-actualizable |
| `evaluacosas.thelaunchpadtlp.education/privacy/` | Política de privacidad |
| `thelaunchpadtlp.education/registry` | Universe Registry con search híbrido AI |

## Arquitectura en una pantalla

```
USUARIO
   ↓
Cloudflare DNS + Worker
   ↓
GitHub Pages (estático, gratis)         Cloud Run app-launchpad-ui (apex)
   ├─ landing                           ├─ Universe Registry + AI search
   ├─ dashboard ──┐                     └─ /api/search-ai → Gemini Flash
   ├─ ayuda/*     │
   ├─ privacy     │
   ├─ biologia-10 │
   └─ sitemap     │
                  │
                  ↓ OAuth ID token
   Cloud Run evaluacosas-submit-handler
   ├─ /submit (público) → 5 sinks redundantes
   ├─ /admin/* (auth) → Firestore reads
   ├─ /admin/grade → Gemini Flash
   └─ /cedula/:cedula → TSE proxy + cache 24h
        ↓
   Sinks (todos idempotentes):
   ├─ Cloud Storage (evaluacosas-entregas)
   ├─ Firestore (submits collection)
   ├─ GitHub espejo privado (evaluacosas-entregas repo)
   ├─ Supabase Postgres
   └─ Linear Issues (Workspace TLP)
```

## Cambios de la sesión (resumen ejecutivo)

### Sprints completados (15+ en una sola sesión)

| # | Sprint | Entregable |
|---|---|---|
| 17 | Hero welcome message | Bloque "central de evaluaciones TLP" en landing |
| 18 | OAuth Client ID | Web App OAuth Client creado + configurado · funciona auth |
| 19 | UX checklists + Apple Liquid Glass | Tokens, SF Pro stack, squircles, glass blur, focus rings |
| 20 | Dashboard discoverability | Globalnav + footer-grid + teachers-callout · /ayuda/ creado |
| 21 | Design system unificado | docs/DESIGN_SYSTEM.md (canon) |
| 22 | Hybrid AI+heurístico search | Operadores + fuzzy + Gemini IA en dashboard + apex |
| 23 | Multi device-view toggle | 16 vistas (iPhone/iPad/Watch/Vision/Mac/Android) auto-detect + override |
| 24 | F.1 GCP audit | docs/GCP_AUDIT_2026-05-06.md · 17 proyectos clasificados |
| 25 | F.2 Etiquetado uniforme | 5/7 proyectos TLP con labels via API v3 |
| 26 | G.1 Design tokens en biology-10 | build.mjs sync tokens · footer + globalnav |
| 27 | G.2 Device-view en biology-10 | /device-view.js + .css en HTML generado |
| 28 | Sitemap auto-actualizable | scripts/build-sitemap.mjs · sitemap.xml + /sitemap/ HTML |

### Fixes críticos
- CORS allow `Authorization` header (server.js)
- Backend env var rename `OAUTH_CLIENT_ID` → `ADMIN_OAUTH_CLIENT_ID`
- IAP OAuth client → Web App OAuth client (rotación)
- Google Pages static doc folders en deploy.yml

### Features mayores desbloqueados
1. **Sign-In with Google funciona** end-to-end (Cliente: 441768184201-ftmct59hdbt3dhsc9u3qfsks7l2125vc)
2. **Switch user + tour interactivo + drawer de ayuda integrado** en dashboard
3. **Búsqueda híbrida** con operadores (`score:>80 passing:no`) + Gemini IA (~$0)
4. **Device toggle universal** con 16 vistas + auto-detect + frames visuales
5. **Sitemap robusto** que se actualiza solo en cada deploy

## Estado por etapa macro

| Etapa | Estado | Nota |
|---|---|---|
| A1-A7 | ✅ completas | Skill v2, passing 80%, anti-skip, TOC, pre-grading, share-link, cierre |
| B | ✅ | GitHub repo + Pages |
| C | ✅ | Subdominio + DNS |
| D | ✅ | Cloud Run + plan Classroom |
| E | ⚙️ | Audit en curso (cleanup post-sesión) |
| **F** | ✅ | **F.1 + F.2 hechos · F.3 (dashboard cross-project) pendiente para próxima sesión** |
| **G** | ✅ | **G.1 + G.2 hechos · G.3 (help drawer estudiante) + G.4 (UX por ítem) pendientes** |
| H | ✅ | Landing + catálogo |

## Próximos sprints recomendados (orden óptimo)

### Sprint 29 — F.3 Dashboard cross-project GCP en apex (~60 min)
Sección "GCP Health" en `/registry/` con:
- Costo último mes por proyecto (Cloud Billing API)
- Servicios Cloud Run activos por proyecto
- Alertas (billing, errores 5xx)
- Cron daily de sync

### Sprint 30 — G.3 Help drawer del estudiante en biology-10 (~30 min)
Drawer lateral igual al del docente pero con tabs:
- Cómo responder
- Mini rúbricas explicadas
- Atajos
- FAQ
- Glosario MEP
- Si me trabo

### Sprint 31 — G.4 Mejoras UX por ítem (~45 min)
- Anillo progreso global conspicuo con %
- Spring animation al guardar
- Smart resume (scroll auto al primer ítem sin guardar)
- Atajos `g`/`s`/`n`
- Badge "borrador local"
- Modo concentración

### Sprint 32 — Fase A del Sprint 18 (privacy/terms/audit middleware) (~45 min)
- Crear `/terms/` (HTML)
- Crear `/ayuda/problemas/` (FAQs)
- Crear `/ayuda/privacidad/` (resumen)
- Audit log middleware en server.js
- Rate limiting middleware

### Sprint 33 — Cleanup final (~30 min)
- Wire `highlight()` en EntityCard del apex
- Remove unused helper `haystackFor` del registry/page.tsx
- Update MEMORY.md y reference_skill_tlp_piqui.md

## Costos
**$0/mes** confirmado. Free tiers cubren todo el uso TLP actual.

## Cosas que tienen que estar siempre
- `dashboard/config.json` con el client_id correcto
- Cloud Run env var `ADMIN_OAUTH_CLIENT_ID` igual al config.json
- `ALLOWED_TEACHERS` en Cloud Run con los emails autorizados
- Pages CNAME apuntando a `evaluacosas.thelaunchpadtlp.education`
- Cloudflare Worker `universe-proxy` activo para apex

## Comandos útiles para mañana

```bash
# Re-rotar OAuth Client (si hace falta):
bash ~/.tlp-tools/tlp-set-oauth-client-id.sh "<new-client-id>"

# Re-build sitemap manualmente:
cd ~/Code/evaluacosas && node scripts/build-sitemap.mjs

# Verificar backend:
curl https://evaluacosas-submit-handler-441768184201.us-central1.run.app/health

# Ver últimas Pages deploys:
gh run list --repo thelaunchpadtlp/evaluacosas --limit 5

# Re-deploy apex:
cd ~/Code/universe-infrastructure/dashboard && \
  gcloud run deploy app-launchpad-ui --source . --region us-east1 \
    --project thelaunchpadtlplabsuniverse --allow-unauthenticated --quiet
```

---
**Cerramos la sesión con cero merge conflicts, cero rojos en CI, cero downtime, todo desplegado.**
